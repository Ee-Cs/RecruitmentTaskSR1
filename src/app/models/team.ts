/**
 * Represents an team in the system.
 * This interface defines the structure of an team object,
 * including name, and score.
 *
 * @interface Team
 * @property {string} name - The name of the team.
 * @property {number} score - The score of the team.
 */
export interface Team {
  name: string;
  score: number;
}
